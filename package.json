{
  "name": "screenregister",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @sr/web dev",
    "build": "pnpm --filter @sr/web build",
    "test": "vitest run",
    "typecheck": "tsc -b --pretty false"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite-node": "^6.0.0",
    "vitest": "^2.1.8"
  },
  "packageManager": "pnpm@10.33.0"
}
