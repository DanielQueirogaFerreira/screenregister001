# Copy to .dev.vars, or just run `pnpm dev:api`, which generates one for you.
# Never commit .dev.vars. Production sets this with: wrangler secret put AUTH_SECRET
AUTH_SECRET = "replace-with-at-least-16-characters"
