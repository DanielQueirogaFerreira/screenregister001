{
  "name": "@sr/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 8787",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply screenregister --local",
    "migrate:remote": "wrangler d1 migrations apply screenregister --remote"
  },
  "dependencies": {
    "@sr/schema": "workspace:*",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "wrangler": "^4.127.1"
  }
}
